solver performance test results
src version: 1.0.0
input file: church.csv
total iterations: 43,574

solver.py line execution statistics:
          def get_all_row_options(row_len: int, el: list) -> list:  # el: elments_ length
      30:     remainder = row_len - (sum(el) + len(el) - 1)  # remainder: how many blank spaces are after the last element 
      30:     cc = remainder
      30:     coordinates = []
     107:     for length in el[:]:
      77:         to_extend = range(cc, (cc + length))
      77:         coordinates.extend(list(to_extend))
      77:         cc += length + 1
          
      30:     options = [coordinates]
      30:     number_of_copies = [0] * remainder
      30:     s_n = 1  # sum of number of copies
     107:     for i in range(len(el)):
     441:         for _ in range(remainder):
    2107:             for coordinates in options[-s_n:]:
    1743:                 c_copy = coordinates[:]
    1743:                 j_a = sum(el[:i])
    1743:                 c_copy.insert(j_a, (c_copy[j_a] - 1))
    1743:                 j_b = sum(el[:(i + 1)])
    1743:                 c_copy.pop(j_b)
    1743:                 options.append(c_copy)
     364:             number_of_copies.append(s_n)
     364:             s_n -= number_of_copies.pop(0)
      77:         s_n = sum(number_of_copies)
          
      30:     return options
          
          
          def solve_row(row: list, options: list) -> bool:
     120:     points = set()
     120:     crosses = set()
    1920:     for i, value in enumerate(row):
    1800:         if value == 1:
     800:             points.add(i)
    1800:         if value == 2:
     571:             crosses.add(i)
              
     120:     to_pop = []
     120:     intersection = set(range(len(row)))
     120:     substraction = set(range(len(row)))
    3223:     for i, option in enumerate(options):
    3103:         coordinates = set(option)
    3103:         if (coordinates & crosses) or not(points <= coordinates):
    1743:             to_pop.append(i)
                  else:
    1360:             intersection = intersection & coordinates
    1360:             substraction = substraction - coordinates
          
    1863:     for i in reversed(to_pop):
    1743:         options.pop(i)
    1045:     for i in intersection:
     925:         row[i] = 1
     791:     for i in substraction:
     671:         row[i] = 2
          
     120:     return True if to_pop else False
          
          
          def solve_field(x_elements: list, y_elements: list) -> list:
       1:     m = len(x_elements)  # number of rows
       1:     n = len(y_elements)  # number of columns
       1:     field = [0] * (m * n)
          
      16:     x_options = [get_all_row_options(n, elements) for elements in x_elements]
      16:     y_options = [get_all_row_options(m, elements) for elements in y_elements]
          
       1:     to_continue = True
       5:     while to_continue:
       4:         to_continue = False
      64:         for i in range(m):  # solve rows
      60:             row = field[i*n:(i + 1)*n]
      60:             if solve_row(row, x_options[i]):
      13:                 to_continue = True
      60:             field[i*n:(i + 1)*n] = row
          
      64:         for i in range(n):  # solve columns
      60:             column = field[i::n]
      60:             if solve_row(column, y_options[i]):
      20:                 to_continue = True
      60:             field[i::n] = column
          
       1:     return field, m, n
